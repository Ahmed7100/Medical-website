name: cicd.yaml

on:
     push:
       branches:
         - main


jobs:
    deploy-to-server:
       runs-on: ubuntu-latest

       steps:
         - name: Checkout code
           uses: actions/setup-node@v3

         - name: display the codes
           runs: ls -la

         - name: dispay more things
           runs: |
             ls -la
             hostnamectl